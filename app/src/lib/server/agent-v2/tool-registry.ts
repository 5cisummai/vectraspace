/**
 * V2 uses the same tools as v1; this module is the extension point for future v2-only tools.
 */
export { getMediaAgent } from '$lib/server/agent/agent';
export { createDefaultTools } from '$lib/server/agent/tools';
